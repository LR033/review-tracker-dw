# Copy this file to `.streamlit/secrets.toml` and fill in your key.
# secrets.toml is gitignored so the key never gets committed.
#
# Used by dashboard/app.py for the "Draft reply with Claude" feature.
# Without it the dashboard still runs; only reply drafting is disabled.
ANTHROPIC_API_KEY = "sk-ant-..."
